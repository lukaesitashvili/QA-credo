version: '3.8'

services:
  wiremock:
    image: wiremock/wiremock:2.35.0
    container_name: wiremock
    ports:
      - "8080:8080"
    volumes:
      - ./mocks:/home/wiremock
    command: "--port 8080 --verbose"
